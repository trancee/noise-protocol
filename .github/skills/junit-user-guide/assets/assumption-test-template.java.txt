import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.junit.jupiter.api.Assumptions.assumeTrue;

import org.junit.jupiter.api.Test;

class ExampleAssumptionTest {

    @Test
    void runsOnlyWhenFeatureFlagIsEnabled() {
        assumeTrue(isFeatureEnabled(), "feature flag must be enabled");

        assertEquals("enabled", computeMode());
    }

    private boolean isFeatureEnabled() {
        throw new UnsupportedOperationException("Replace with environment or configuration check");
    }

    private String computeMode() {
        throw new UnsupportedOperationException("Replace with production call");
    }
}
